---

docker_edition: 'ce'
# https://docs.docker.com/engine/admin/logging/overview/
docker_log_driver: journald
docker_selinux: true
docker_selinux_container_cgroup: true

# storage

docker_volume_size: 95%VG
# possible values: overlay2 or vfs
docker_storage_driver: overlay2
docker_volume_name: docker

# networking

docker_ipv4_cidr: "192.168.0.0/16"
docker_ipv6_cidr: "fddd:282c:fadc:66a4::/64"
docker_icc: true
docker_ipv6: true
docker_userland_proxy: false
docker_metadata: true

# testing

docker_role_debug: false
